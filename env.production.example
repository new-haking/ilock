# AuthLook Backend Configuration - PRODUÇÃO
# Use este arquivo como referência para configurar o .env em produção
# IMPORTANTE: NUNCA commite o arquivo .env real com credenciais!

# Database
# Para produção, SEMPRE use PostgreSQL:
DATABASE_URL=postgresql://user:password@localhost:5432/authlook
# Ou para PostgreSQL remoto:
# DATABASE_URL=postgresql://user:password@host:5432/authlook

# Security
# IMPORTANTE: Gere uma chave secreta forte e única!
# Use: openssl rand -hex 32
SECRET_KEY=GERE_UMA_CHAVE_SECRETA_FORTE_AQUI_COM_OPENSSL_RAND_HEX_32

# OAuth - Google
# Obtenha em: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=YOUR_GOOGLE_CLIENT_ID_HERE.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=YOUR_GOOGLE_CLIENT_SECRET_HERE
# Redirect URI deve apontar para o backend em produção:
GOOGLE_REDIRECT_URI=https://api.authlook.cc/auth/google/callback

# OAuth - Discord
# Obtenha em: https://discord.com/developers/applications
DISCORD_CLIENT_ID=YOUR_DISCORD_CLIENT_ID_HERE
DISCORD_CLIENT_SECRET=YOUR_DISCORD_CLIENT_SECRET_HERE
# Redirect URI deve apontar para o backend em produção:
DISCORD_REDIRECT_URI=https://api.authlook.cc/auth/discord/callback

# Frontend URL (para redirecionar após OAuth)
FRONTEND_URL=https://authlook.cc

# CORS (separar por vírgula)
# Inclua todos os domínios que vão acessar a API
CORS_ORIGINS=https://authlook.cc,https://www.authlook.cc
